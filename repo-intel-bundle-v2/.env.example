# GitHub Personal Access Token (Required)
GITHUB_TOKEN=your_github_token_here

# Optional: GitHub Enterprise API URL (e.g., https://github.example.com/api/v3)
# Leave empty to default to https://api.github.com
GITHUB_API_BASE_URL=

# Fallback default branch if API doesn't return it
GITHUB_DEFAULT_BRANCH_FALLBACK=main

# Output directory for generated reports
OUTPUT_DIR=outputs
